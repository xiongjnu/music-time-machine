# 音乐时光机器 — 完整开发文档

> **Music Time Machine** — 以年代为轴的音乐时光探索器  
> 版本：v2.0 | 最后更新：2026-05 | 变更：全部Phase完成 + 测试 + 生产部署

---

## 目录

1. [项目概述](#1-项目概述)
2. [功能需求规格](#2-功能需求规格)
3. [UI/UX 设计规格](#3-uiux-设计规格)
4. [技术架构](#4-技术架构)
5. [数据架构与策展数据](#5-数据架构与策展数据)
6. [音频播放引擎](#6-音频播放引擎)
7. [安全与风控](#7-安全与风控)
8. [GitHub参考项目研究](#8-github参考项目研究)
9. [项目结构](#9-项目结构)
10. [开发阶段与里程碑](#10-开发阶段与里程碑)
11. [风险与对策](#11-风险与对策)
12. [附录](#12-附录)

---

## 1. 项目概述

### 1.1 一句话定义

一个以时间轴为核心的音乐探索器，用户通过滑动年代 → 选择地区 → 筛选风格（或Mix混合），快速发现和收听每个时代的经典音乐。

### 1.2 核心体验

**滑动 → 发现 → 聆听**，三步完成，零门槛。

### 1.3 项目信息

| 项 | 值 |
|---|---|
| 项目名 | music-time-machine |
| 仓库名 | `github.com/<username>/music-time-machine` |
| 开源协议 | MIT |
| 初期年代范围 | 1970 - 2000（7个5年跨度） |
| 初期地区 | 欧美 / 香港 / 大陆（预留台湾/日本/韩国） |
| 初期风格 | Mix / 摇滚 / 民谣 / 流行 / R&B灵魂 / 电子（预留古典/嘻哈/爵士） |

### 1.4 差异化定位

市场上已有项目对比：

| 已有项目 | 做了什么 | 我们的差异 |
|---------|---------|-----------|
| Music Time Machine (GitHub) | Billboard历史榜单+日期选择 | 仅Billboard，无地区/风格筛选，无中国平台 |
| Music Timeline Explorer | D3.js时间线+流派演化可视化 | 没有播放功能，纯可视化 |
| YesPlayMusic (32.8k⭐) | 高颜值网易云第三方播放器 | 没有时间线/年代探索功能 |
| MusicFreeDesktop (8.3k⭐) | 插件化多平台音乐播放器 | 没有时间线，纯播放器 |

**市场空白**：年代探索 × 中国音乐平台 × 地区风格筛选 × 直接播放 = 无人做过

---

## 2. 功能需求规格

### 2.1 功能清单

| 编号 | 功能 | 优先级 | Phase | 说明 |
|------|------|--------|-------|------|
| F01 | 网易云二维码扫码登录 | P0 | 1 | 唯一登录方式，Cookie仅存内存 |
| F02 | 时间滑块选择年代 | P0 | 3 | 5年一格，1970-2000 |
| F03 | 地区Tab切换 | P0 | 3 | 欧美/香港/大陆 |
| F04 | 风格筛选 | P0 | 3 | Mix(默认)/摇滚/民谣/流行/R&B/电子 |
| F05 | Mix混合模式 | P0 | 3 | 跨风格随机组合10首，显示风格标签 |
| F06 | 歌曲列表展示 | P0 | 3 | Top10，显示歌名/歌手/时长/风格标签 |
| F07 | 点击即播 | P0 | 3 | HTML5 Audio完整播放 |
| F08 | 底部播放器 | P0 | 3 | 进度条/音量/播放暂停 |
| F09 | VIP歌曲处理 | P0 | 3 | 提示+自动切换音源（Phase 3 Mock / Phase 5真实接入） |
| F10 | 自动播放下一首 | P1 | 3 | 当前歌曲播完自动播放列表下一首 |
| F11 | 策展数据验证 | P0 | 2 | 启动时验证歌曲有效性+自动替补 |
| F12 | QQ音乐音源真实接入 | P2 | 5 | ✅ 调用QQ音乐API，vkey→CDN URL完整链路 |
| F13 | 年代扩展 | P2 | 5 | ✅ 1970-2000，7个年代（与原范围一致） |
| F14 | 地区扩展(台湾/日本/韩国) | P2 | 5 | ✅ 全部6个地区 |
| F15 | 风格扩展(古典/嘻哈/爵士) | P2 | 5 | ✅ 全部9个风格（含Mix） |
| F16 | 年代二级年份选择器 | P2 | 5 | ✅ 5年格→精确年份子选择 |
| F17 | 测试覆盖 | P0 | 5+ | ✅ 63个测试，89%+覆盖率，node:test + supertest |
| F18 | PM2本地生产部署 | P0 | 5+ | ✅ macOS launchd开机自启，端口3000 |

### 2.2 非功能需求

| 编号 | 需求 | 指标 |
|------|------|------|
| NF01 | 启动速度 | 双击脚本后5秒内打开浏览器 |
| NF02 | 歌曲列表加载 | 切换年代/地区/风格后1秒内显示 |
| NF03 | 播放启动延迟 | 点击播放后2秒内开始播放 |
| NF04 | 内存占用 | 后端服务<200MB |
| NF05 | 浏览器兼容 | Chrome 90+, Safari 15+, Firefox 90+ |
| NF06 | 无离线模式 | 必须联网+登录 |
| NF07 | 错误日志 | 播放失败/API错误有分级日志，前端错误自动上报后端 |
| NF08 | 数据冷启动 | 首次启动加载状态<3秒显示，超时10秒降级为有限功能 |
| NF09 | Safari兼容 | 播放需用户手势触发，自动播放限制优雅降级 |

### 2.3 不做的功能

- 用户收藏/歌单功能
- 播放历史记录
- 账号密码登录（仅二维码）
- 离线模式
- 社交分享
- 歌词显示（后续版本考虑）
- Electron桌面端（后续版本考虑）

---

## 3. UI/UX 设计规格

### 3.1 页面流程

```
启动应用 → 扫码登录页 → 主界面
```

仅两个页面，无其他跳转。

### 3.2 页面1：扫码登录

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│          ✦  星空粒子动画背景  ✦                      │
│                                                     │
│              🎵 音乐时光机器                         │
│            Music Time Machine                       │
│                                                     │
│     ┌───────────────────────────────────┐           │
│     │                                   │           │
│     │    ┌─────────────────────┐        │           │
│     │    │                     │        │           │
│     │    │    网易云二维码       │        │           │
│     │    │    (实时刷新)        │        │           │
│     │    │                     │        │           │
│     │    └─────────────────────┘        │           │
│     │                                   │           │
│     │    请打开网易云音乐APP扫码登录      │           │
│     │                                   │           │
│     │    ● 等待扫码中...                 │           │
│     │                                   │           │
│     └───────────────────────────────────┘           │
│                                                     │
│              Powered by 网易云音乐                   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**交互规则**：
- 二维码每30秒自动刷新（网易云机制）
- 扫码成功后自动跳转主界面，无额外确认
- Cookie仅存内存，关闭浏览器即失效
- 无"记住登录"功能

### 3.3 页面2：主界面

```
┌──────────────────────────────────────────────────────────────┐
│  🎵 音乐时光机器                          👤 已登录  [退出]  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ✦ 星空粒子 + 光带流动背景 ✦                                 │
│                                                              │
│  ◄━━━━━━━●━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━►      │
│  1970    1975    1980    1985    1990    1995    2000         │
│          ▲当前                                               │
│       ┌──┴──┐                                                │
│       │1975 │  ← 年代标签跟随滑块浮动显示                     │
│       └─────┘                                                │
│                                                              │
│  ┌──────────┐  ┌──────┐  ┌──────┐                          │
│  │  🌍 欧美  │  │🇭🇰 香港│  │🇨🇳 大陆│   ← 地区选择           │
│  └──────────┘  └──────┘  └──────┘                          │
│                                                              │
│  [Mix🎲]  [🎸摇滚]  [🎻民谣]  [🎤流行]  [🎶R&B]  [🎹电子]   │
│    ▲默认                                                     │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─ # ─┬────────────────────┬──────────┬───────┬──────┐     │
│  │  1  │ Bohemian Rhapsody  │  Queen   │🎸摇滚  │ 5:55 │     │
│  │  2  │ Blowin' in the Wind│  Dylan   │🎻民谣  │ 2:48 │     │
│  │  3  │ Superstition       │  Wonder  │🎶R&B   │ 4:26 │     │
│  │  4  │ Hotel California   │  Eagles  │🎸摇滚  │ 6:30 │     │
│  │  5  │ Stayin' Alive      │ Bee Gees │🎤流行  │ 4:44 │     │
│  │  6  │ ...                │          │       │      │     │
│  │ 10  │                    │          │       │      │     │
│  └─────┴────────────────────┴──────────┴───────┴──────┘     │
│                                                              │
│  👑 = VIP歌曲，点击时提示并自动切换音源                       │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ▶ ━━━━━━━●━━━━━━━━━━━━  2:15 / 5:55                       │
│  🎵 Bohemian Rhapsody - Queen                   🔊 ━━●━━    │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 3.4 Mix模式 vs 指定风格模式

| 行为 | Mix模式（默认） | 指定风格模式 |
|------|----------------|-------------|
| **选中状态** | 默认选中 | 用户手动点击 |
| **歌曲来源** | 该年代×地区下所有风格，每类随机抽2首，组成10首 | 该年代×地区×风格的Top10 |
| **排序** | 随机打乱 | 按热度排序 |
| **风格标签** | 每首歌显示风格小标签（🎸🎻🎤🎶🎹） | 风格统一，不显示标签 |
| **切换行为** | 切回Mix时重新随机组合 | 固定排序 |

### 3.5 交互流程

```
                     打开应用
                        │
                        ▼
                  ┌───────────┐
                  │  扫码登录  │ ← 唯一入口
                  └─────┬─────┘
                        │ 扫码成功
                        ▼
              ┌───────────────────┐
              │     主界面         │
              │  默认: 1970/欧美/Mix│
              └────────┬──────────┘
                       │
          ┌────────────┼────────────┐
          ▼            ▼            ▼
    ┌──────────┐ ┌──────────┐ ┌──────────┐
    │ 拖动滑块  │ │ 切换地区  │ │ 切换风格  │
    └────┬─────┘ └────┬─────┘ └────┬─────┘
         │            │            │
         └────────────┼────────────┘
                      ▼
            ┌──────────────────┐
            │  请求后端API      │
            │  GET /api/songs? │
            │  era=1975&       │
            │  region=western& │
            │  genre=mix       │
            └────────┬─────────┘
                     ▼
            ┌──────────────────┐
            │  返回Top10歌曲    │
            │  列表刷新(动画)   │
            └────────┬─────────┘
                     │
                     ▼
            ┌──────────────────┐
            │  用户点击播放     │
            └────────┬─────────┘
                     │
              ┌──────┼──────┐
              ▼      ▼      ▼
          有URL    VIP限制   无版权
          直接播   提示+切源  提示+切源
           ✅      →QQ源    →QQ源
                    ✅/❌     ✅/❌
```

### 3.6 交互细节规则

| 操作 | 效果 | 动画 |
|------|------|------|
| 拖动时间滑块 | 实时切换年代，歌曲列表刷新 | 光带加速流动，列表淡入淡出 |
| 切换地区 | 歌曲列表按新地区刷新 | 列表向左滑出，新列表从右滑入 |
| 切换风格 | 歌曲列表按新风格筛选 | Chip高亮切换，列表平滑更新 |
| 点击播放 | 底部播放器开始播放 | 播放器展开，进度条启动 |
| 切换年代/地区/风格时正在播放 | **播放不中断**，继续播放当前歌曲 | 列表刷新但底部播放器稳定 |
| 当前歌曲播完 | 自动播放列表下一首 | 无缝切换 |
| VIP歌曲点击 | Toast提示"VIP歌曲，正在切换音源" | Toast 2秒后消失 |
| 无音源歌曲 | 显示"暂无音源"，1.5秒后自动跳下一首 | Toast提示 |
| 点击退出 | 清除内存Cookie，回到登录页 | 页面过渡 |

### 3.7 年代二级年份选择器（Phase 5）

> 优先级P2，Phase 5实现。第一版（Phase 3）5年格已够用。

**交互规格**：

```
在主年代滑块下方增加二级年份选择器（展开状态）：

  ◄━━━━━━━●━━━━━━━━━━━━━━━━━━━►      ← 5年格滑块
  1970    1975    1980    1985   1990
          ▲ 选中1980

  [1980] [1981] [1982] [1983] [1984] ← 二级年份选择器
    ▲ 默认选中该格的起始年份
```

| 规则 | 说明 |
|------|------|
| 触发条件 | 用户选中任意5年格后，二级选择器自动展开 |
| 年份范围 | 该5年格内的5个具体年份（如1980格显示1980-1984） |
| 默认选中 | 该格的起始年份 |
| 交互 | 点击具体年份 → 请求加入 `year` 参数筛选 |
| 收起/展开 | 可手动收起，默认展开 |
| API变更 | `GET /api/songs?era=1980&year=1982&region=western&genre=mix` |
| 后端筛选 | 若指定year，在active列表中精确匹配该年份的歌曲 |

### 3.8 星际隧道视觉规格

#### 背景层（Canvas动画）

```
┌─────────────────────────────────────┐
│  ·  ✦    ·       ✦   ·    ·  ✦     │  ← 星空粒子
│     ·    ✦    ·       ·    ✦       │     (随机分布，缓慢闪烁漂移)
│  ══════════════════════════════════  │  ← 时光光带
│     ·       ✦    ·    ✦     ·      │     (水平方向持续流动)
│  ✦    ·       ·    ✦      ·   ✦   │
│      ✦    ·      ✦    ·     ✦     │
└─────────────────────────────────────┘
```

- **小星星(·)**：200+个，随机分布，0.5-2px，opacity 0.3-0.8，缓慢闪烁
- **亮星(✦)**：30+个，2-4px，带径向渐变光晕，opacity 0.6-1.0
- **光带(══)**：3-5条水平光带，有弯曲波动，持续从右向左流动

#### 光带颜色随年代变化

| 年代 | 光带主色 | 氛围 |
|------|---------|------|
| 1970 | 暖橙 #E8833A / 棕 #8B5E3C | 复古温暖 |
| 1975 | 深红 #C62828 / 金 #FFB300 | 热烈激进 |
| 1980 | 霓虹粉 #FF1493 / 蓝紫 #7B68EE | 迪斯科迷幻 |
| 1985 | 电蓝 #00BFFF / 紫 #9400D3 | 合成器波 |
| 1990 | 深蓝 #1A237E / 绿 #00C853 | Grunge/数字 |
| 1995 | 银蓝 #90CAF9 / 白 #E0E0E0 | 千禧前夜 |
| 2000 | 银白 #F5F5F5 / 冰蓝 #B3E5FC | 未来感 |

#### UI元素视觉风格

| 元素 | 风格 |
|------|------|
| 整体底色 | 深空黑 #0A0E17 |
| 卡片/面板 | 半透明毛玻璃 `rgba(15,25,50,0.7)` + `backdrop-filter: blur(20px)` |
| 文字 | 主文字 #E8EAED，次要文字 #9AA0A6 |
| 滑块 | 透明轨道 + 发光圆点，光晕随光带颜色变化 |
| 地区Tab | 未选中：半透明边框；选中：发光填充 |
| 风格Chip | 未选中：虚线边框；选中：实色填充+发光 |
| 播放按钮 | 圆形，播放时脉冲发光动画 |
| 进度条 | 细线 + 发光圆点指示器 |
| VIP标记👑 | 金色 #FFD700 小图标 |

---

## 4. 技术架构

### 4.1 整体架构图

```
┌─────────────────────────────────────────────────────┐
│                  用户浏览器                          │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │            前端 (纯HTML/CSS/JS)              │   │
│  │                                             │   │
│  │  ┌───────────┐  ┌────────┐  ┌───────────┐  │   │
│  │  │ UI控制层   │  │ 播放器  │  │ Canvas动画 │  │   │
│  │  │ 滑块/Tab/  │  │ Audio  │  │ 星空/光带  │  │   │
│  │  │ Chip/列表  │  │ Engine │  │ Tunnel     │  │   │
│  │  └─────┬─────┘  └───┬────┘  └───────────┘  │   │
│  │        │            │                       │   │
│  └────────┼────────────┼───────────────────────┘   │
│           │            │                           │
└───────────┼────────────┼───────────────────────────┘
            │ HTTP API   │ Audio Stream URL
            ▼            ▼
┌─────────────────────────────────────────────────────┐
│            本地后端服务 (Node.js)                     │
│            端口: localhost:3000                      │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │              Express 路由层                    │  │
│  │  /api/auth/*    /api/songs/*   /api/play/*   │  │
│  └────────────────────┬─────────────────────────┘  │
│                       │                            │
│  ┌────────────────────┼─────────────────────────┐  │
│  │            业务逻辑层                          │  │
│  │                                               │  │
│  │  ┌──────────────┐  ┌──────────────────────┐  │  │
│  │  │ 登录管理器    │  │   AI归类引擎          │  │  │
│  │  │ · 二维码生成  │  │ · 关键词搜索聚合      │  │  │
│  │  │ · 扫码轮询    │  │ · 元数据提取          │  │  │
│  │  │ · Cookie管理  │  │ · 年代/地区/风格归类  │  │  │
│  │  └──────────────┘  │ · 热度排序Top10       │  │  │
│  │                    └──────────────────────┘  │  │
│  │  ┌──────────────┐  ┌──────────────────────┐  │  │
│  │  │ 播放URL获取   │  │ 缓存管理器           │  │  │
│  │  │ · 网易云优先  │  │ · 歌曲数据缓存       │  │  │
│  │  │ · QQ音乐回退  │  │ · 播放URL缓存        │  │  │
│  │  │ · VIP检测     │  │ · 搜索结果缓存       │  │  │
│  │  └──────────────┘  └──────────────────────┘  │  │
│  └───────────────────────────────────────────────┘  │
│                       │                            │
│  ┌────────────────────┼─────────────────────────┐  │
│  │            音源适配层                           │  │
│  │                                               │  │
│  │  ┌──────────────────┐  ┌──────────────────┐  │  │
│  │  │ 网易云适配器      │  │ QQ音乐适配器      │  │  │
│  │  │ NeteaseAdapter   │  │ QQMusicAdapter    │  │  │
│  │  │ (基于Enhanced版)  │  │ (预留，Phase5)    │  │  │
│  │  └──────────────────┘  └──────────────────┘  │  │
│  └───────────────────────────────────────────────┘  │
│                       │                            │
│  ┌────────────────────▼─────────────────────────┐  │
│  │    NeteaseCloudMusicApiEnhanced               │  │
│  │    (npm: @neteasecloudmusicapienhanced/api)   │  │
│  │    端口: localhost:4000 (内部)                 │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 4.2 技术选型

| 层面 | 技术 | 版本 | 理由 |
|------|------|------|------|
| **后端运行时** | Node.js | 18+ LTS | NeteaseCloudMusicApiEnhanced依赖 |
| **后端框架** | Express | 4.x | 轻量，与网易云API一致 |
| **网易云API** | NeteaseCloudMusicApiEnhanced | 4.32+ | 原版已归档，此为社区活跃fork |
| **前端** | 纯HTML/CSS/JS | - | 零构建依赖，双击可开 |
| **音频播放** | HTML5 Audio API | - | 原生支持，无需第三方库 |
| **Canvas动画** | 原生Canvas 2D API | - | 星空粒子+光带，纯JS实现 |
| **HTTP请求(前端)** | Fetch API | - | 原生，无需Axios |

### 4.3 前后端通信API设计

#### 认证相关

```
POST /api/auth/qrcode/key
  → 获取二维码Key
  → Response: { code: 200, data: { unikey: "xxx" } }

POST /api/auth/qrcode/create
  Body: { key: "xxx", qrimg: true }
  → 生成二维码图片
  → Response: { code: 200, data: { qrimg: "data:image/png;base64,..." } }

POST /api/auth/qrcode/check
  Body: { key: "xxx" }
  → 检查扫码状态
  → Response: { code: 800/801/802/803, message: "..." }
     800=过期, 801=等待扫码, 802=待确认, 803=成功

GET /api/auth/status
  → 检查登录状态
  → Response: { code: 200, data: { isLogin: true, profile: {...} } }

POST /api/auth/logout
  → 退出登录，清除内存Cookie
```

#### 数据相关

```
GET /api/songs?era=1975&region=western&genre=mix
  → 获取指定年代/地区/风格的歌曲列表
  → Response: {
      code: 200,
      data: {
        era: "1975",
        region: "western",
        genre: "mix",
        songs: [
          {
            id: "xxx",
            title: "Bohemian Rhapsody",
            artist: "Queen",
            artistId: "yyy",
            album: "A Night at the Opera",
            year: 1975,
            genre: "rock",
            duration: 355000,
            isVip: false,
            coverUrl: "https://..."
          },
          ...
        ]
      }
    }

GET /api/songs/refresh?era=1975&region=western
  → 强制重新搜索归类（刷新缓存）
  → Response: 同上
```

#### 播放相关

```
GET /api/play/url?id=xxx
  → 获取播放URL
  → Response: {
      code: 200,
      data: {
        url: "https://...",
        source: "netease",   // 或 "qqmusic"
        isVip: false,
        br: 320000
      }
    }
  → 如果网易云无可用URL，自动尝试QQ音乐
  → 如果都无: { code: 404, message: "暂无音源" }
```

#### 工具接口

```
GET /api/eras
  → 获取可用年代列表
  → Response: { code: 200, data: ["1970","1975","1980","1985","1990","1995","2000"] }

GET /api/regions
  → 获取可用地区列表
  → Response: { code: 200, data: [{id:"western",name:"欧美"},{id:"hk",name:"香港"},{id:"mainland",name:"大陆"}] }

GET /api/genres
  → 获取可用风格列表
  → Response: { code: 200, data: [{id:"mix",name:"Mix",icon:"🎲"},{id:"rock",name:"摇滚",icon:"🎸"},...] }
```

---

## 5. 数据架构与策展数据

### 5.1 数据模型

```json
{
  "Song": {
    "id": "string",          // 平台歌曲ID
    "platform": "string",    // "netease" | "qqmusic"
    "title": "string",       // 歌曲名
    "artist": "string",      // 歌手名
    "artistId": "string",    // 歌手平台ID
    "album": "string",       // 专辑名
    "year": "number",        // 发行年份
    "era": "string",         // 归属年代 "1970"|"1975"|...|"2000"
    "region": "string",      // "western"|"hk"|"mainland"
    "genre": "string",       // "rock"|"folk"|"pop"|"rnb"|"electronic"
    "duration": "number",    // 时长(ms)
    "isVip": "boolean",      // 是否VIP歌曲
    "coverUrl": "string",   // 封面图URL
    "genreTag": "string"     // 风格标签emoji "🎸"|"🎻"|"🎤"|"🎶"|"🎹"
  }
}
```

### 5.2 策展数据方案

> **设计决策**：放弃AI归类引擎，改用策展数据。原因见附录12.1决策记录#17。
>
> 核心思路：每个「年代×地区×风格」格子由人工/LLM预先选曲，存入JSON文件。运行时零推理、零搜索，确定性优先。

#### 5.2.1 JSON数据结构

```json
{
  "version": 1,
  "lastUpdated": "2025-07-XX",
  "slots": {
    "1975-western-rock": {
      "era": "1975",
      "region": "western",
      "genre": "rock",
      "active": [
        {
          "id": "1293886117",
          "platform": "netease",
          "title": "Bohemian Rhapsody",
          "artist": "Queen",
          "artistId": "104512",
          "album": "A Night at the Opera",
          "year": 1975,
          "duration": 355000,
          "isVip": false,
          "coverUrl": "",
          "genreTag": "🎸"
        }
      ],
      "reserve": [
        // 候补歌曲，active失效时自动顶入
      ]
    }
  }
}
```

**字段说明**：

| 字段 | 必填 | 说明 |
|------|------|------|
| `active` | 是 | 当前展示的歌曲列表，建议10首 |
| `reserve` | 否 | 候补池，建议5-10首，active歌曲失效时自动顶入 |
| `id` | 是 | 网易云歌曲ID（纯数字字符串） |
| `platform` | 是 | 音源平台，初期全为 `"netease"` |
| `genreTag` | 是 | 风格emoji，Mix模式下显示在歌曲行 |

#### 5.2.2 歌曲ID获取策略

| 优先级 | 格子类型 | ID获取方式 | 准确度 |
|--------|---------|-----------|--------|
| 高 | 首批13个高密度格子 | 手动在网易云搜索确认 | 100% |
| 中 | 数据密度中等格子 | `search-helper.js` 歌名+歌手+专辑三段组合搜索 → 人工抽检 | ~95% |
| 低 | 低频/长尾格子 | 脚本批量搜索 + 置信度标注 → 低置信标记待审 | ~85% |

**首批13个高密度格子（手填验证）**：

| 序号 | 格子Key | 说明 |
|------|---------|------|
| 1 | `1975-western-rock` | 经典摇滚黄金期 |
| 2 | `1975-western-pop` | 70s流行 |
| 3 | `1975-hk-pop` | 粤语金曲爆发期（《上海滩》《半斤八两》） |
| 4 | `1980-western-rock` | New Wave / Post-Punk |
| 5 | `1980-western-pop` | 迪斯科与流行 |
| 6 | `1980-western-electronic` | 合成器浪潮 |
| 7 | `1980-hk-pop` | 粤语流行鼎盛 |
| 8 | `1985-western-electronic` | House / Techno萌芽 |
| 9 | `1985-western-pop` | 80s中后期流行 |
| 10 | `1990-western-pop` | Boy Band / Diva时代 |
| 11 | `1990-western-rock` | Grunge / Alt-Rock |
| 12 | `1990-hk-pop` | 四大天王 |
| 13 | `1990-mainland-rock` | 崔健/黑豹/唐朝，大陆摇滚黄金期 |

#### 5.2.3 年代×地区×风格矩阵（数据密度参考）

> 以下矩阵用于指导数据填充优先级，★越多表示该格子越容易找到足够经典歌曲。

| 年代 | 欧美 | 香港 | 大陆 |
|------|------|------|------|
| 1970 | 摇滚★★★★★ / 流行★★★★ / R&B★★★★ / 民谣★★★ / 电子★ | 流行★★★★ / 其他★ | 流行★★ / 其他☆ |
| 1975 | 摇滚★★★★★ / 流行★★★★ / R&B★★★★ / 民谣★★★★ / 电子★★ | 流行★★★★ / 其他★ | 流行★★ / 其他☆ |
| 1980 | 摇滚★★★★★ / 流行★★★★★ / R&B★★★★ / 民谣★★★ / 电子★★★ | 流行★★★★★ / 摇滚★★ / 其他★ | 流行★★★ / 摇滚★ / 其他☆ |
| 1985 | 摇滚★★★★★ / 流行★★★★ / R&B★★★★ / 民谣★★★ / 电子★★★ | 流行★★★★★ / 摇滚★★ / 其他★★ | 流行★★★ / 摇滚★★ / 民谣★ |
| 1990 | 摇滚★★★★★ / 流行★★★★★ / R&B★★★★ / 民谣★★★★ / 电子★★★ | 流行★★★★★ / 摇滚★★★ / 其他★★ | 流行★★★★ / 摇滚★★★ / 民谣★★ |
| 1995 | 摇滚★★★★ / 流行★★★★★ / R&B★★★★★ / 民谣★★★ / 电子★★★★ | 流行★★★★★ / 摇滚★★★ / 其他★★ | 流行★★★★ / 摇滚★★★ / 民谣★★ |
| 2000 | 摇滚★★★★ / 流行★★★★★ / R&B★★★★★ / 民谣★★★★ / 电子★★★★★ | 流行★★★★★ / 摇滚★★★ / 其他★★★ | 流行★★★★★ / 摇滚★★★★ / 民谣★★★ |

> ★★★★★ = 可轻松找到10首以上经典  
> ☆ = 几乎无对应作品，该格子将标记为空，前端自动隐藏该风格标签

### 5.3 启动时验证机制

```
启动应用
  │
  ▼
读取 data/songs.json
  │
  ▼
遍历所有 active 歌曲 ID
  │
  ▼
批量调用 /song/detail 验证（并发控制：5个/批）
  │
  ├─ <5秒完成 → 全量可用，无额外提示
  ├─ 5-10秒 → 先返回已验证的歌曲，后台继续验证剩余
  ├─ >10秒超时 → 不等待验证结果，直接展示原始JSON数据（未验证状态）
  │    前端显示"部分歌曲状态待确认" Toast
  │
  │   对每首歌的验证结果:
  ├─ 返回有效 → 保留
  ├─ 返回404/无数据 → 标记失效
  │     │
  │     ▼
  │   从 reserve 中取候补顶入 active
  │     │
  │     ▼
  │   更新内存缓存（不修改JSON文件）
  │
  └─ reserve 也耗尽 → 该格子标记 dataInsufficient: true
                        前端显示"该风格暂无足够歌曲"
```

**关键设计**：

| 设计项 | 方案 | 理由 |
|--------|------|------|
| 验证频率 | 仅启动时一次 | 避免频繁请求触发风控 |
| 结果存储 | 写入内存缓存，不修改JSON | 避免文件写入磨损；JSON是策展源文件，不应被程序修改 |
| 并发控制 | 5个/批，批间间隔500ms | 控制请求频率，不触发限速 |
| 超时处理 | <5秒全量可用 / 5-10秒分批返回 / >10秒直接展示原始JSON | 防止验证过程阻塞用户操作 |
| 失效日志 | 记录到日志文件 | 便于后续人工补充数据 |
| 降级策略 | 数据不足时坦诚提示用户 | 不回退到实时搜索，坚持策展纯度 |

#### 5.3.1 缓存清理策略

| 数据 | 位置 | 清理策略 |
|------|------|---------|
| 策展数据JSON | `data/songs.json` | 手动管理（项目文件，不被程序修改） |
| 验证结果缓存 | 内存 | 每次重启时重建，不清除文件 |
| 播放URL缓存 | 内存(`cache.js`) | 2小时TTL自动过期 |
| 应用日志 | `server/logs/` | 保留最近30天，自动删除早于30天的文件 |
| 工具脚本临时文件 | 无 | 不产生临时文件 |

### 5.4 Mix模式算法

```javascript
function generateMixList(slots, era, region) {
  // 从该年代×地区下所有风格的 active 列表中取歌
  const allSongs = [];
  for (const [key, slot] of Object.entries(slots)) {
    if (slot.era === era && slot.region === region && slot.active) {
      allSongs.push(...slot.active.map(s => ({ ...s, genre: slot.genre })));
    }
  }

  // 每个风格最多取2首
  const byGenre = groupBy(allSongs, 'genre');
  const selected = [];
  for (const [genre, songs] of Object.entries(byGenre)) {
    selected.push(...songs.slice(0, 2));
  }

  // 补足10首（如果风格数<5）
  if (selected.length < 10) {
    const selectedIds = new Set(selected.map(s => s.id));
    const remaining = allSongs
      .filter(s => !selectedIds.has(s.id));
    selected.push(...remaining.slice(0, 10 - selected.length));
  }

  // 随机打乱顺序
  return shuffle(selected.slice(0, 10));
}
```

---

## 6. 音频播放引擎

### 6.1 播放URL获取流程

```
用户点击歌曲
      │
      ▼
前端请求 GET /api/play/url?id=xxx
      │
      ▼
后端处理:
┌─────────────────────────────┐
│ 1. 检查URL缓存              │
│    有缓存且未过期 → 直接返回 │
│                             │
│ 2. 请求网易云 /song/url     │
│    ├─ 返回有效URL → 缓存+返回│
│    ├─ VIP限制(freeTrialInfo) │
│    │   → 标记isVip=true     │
│    │   → 尝试QQ音乐源       │
│    └─ 无URL(null)            │
│        → 尝试QQ音乐源       │
│                             │
│ 3. QQ音乐源(Phase5)         │
│    ├─ 返回有效URL → 缓存+返回│
│    └─ 也无 → 返回404        │
└─────────────────────────────┘
```

### 6.2 前端播放器架构

```javascript
class Player {
  constructor() {
    this.audio = new Audio();
    this.playlist = [];      // 当前歌曲列表
    this.currentIndex = -1;  // 当前播放索引
    this.isPlaying = false;
  }

  async play(song) {
    try {
      // 请求播放URL
      const res = await fetch(`/api/play/url?id=${song.id}`);
      const data = await res.json();

      if (data.code === 404) {
        this.showToast('暂无音源，跳转下一首');
        this.playNext();
        return;
      }

      if (data.data.isVip && data.data.source === 'netease') {
        this.showToast('VIP歌曲，已切换音源');
      }

      this.audio.src = data.data.url;
      this.audio.play();
      this.isPlaying = true;
    } catch (err) {
      this.showToast('播放失败，跳转下一首');
      this.playNext();
    }
  }

  playNext() { /* 播放列表下一首 */ }
  playPrev() { /* 播放列表上一首 */ }
  pause()    { /* 暂停 */ }
  seek(pct)  { /* 跳转进度 */ }
  setVolume(v) { /* 设置音量 */ }
}
```

### 6.3 VIP歌曲处理策略

| 情况 | 前端行为 | 后端行为 |
|------|---------|---------|
| 网易云返回完整URL | 正常播放 | 返回 `{source:"netease", isVip:false}` |
| 网易云返回VIP试听 | Toast提示+标记👑 | 检测`freeTrialInfo`，触发Mock降级源 |
| 网易云无版权 | 自动尝试Mock降级源 | 触发Mock降级源 |
| Mock降级源（Phase 3） | 显示Toast"已切换QQ音乐源" | 返回另一首可播的非VIP歌曲URL `{source:"qqmusic", url:"..."}` |
| QQ音乐真实源（Phase 5） | 正常播放 | 调用QQ音乐API获取真实播放链接 |
| 全部无音源 | Toast"暂无音源"+1.5秒后跳下一首 | 返回 `{code:404}` |

### 6.4 错误处理与日志

#### 6.4.1 错误分级

| 级别 | 范围 | 处理方式 |
|------|------|---------|
| INFO | API调用成功/播放正常 | 不记录 |
| WARN | 歌曲失效/替补顶入/播放URL过期 | 写入日志文件+更新内存缓存 |
| ERROR | 播放失败/网络断开/API返回异常 | 写入日志文件+前端Toast提示 |
| FATAL | 策展数据加载失败/后端服务异常 | 日志+前端显示错误页 |

#### 6.4.2 日志文件

- 写入 `server/logs/` 目录（受gitignore保护）
- 按天轮转：`app-YYYY-MM-DD.log`
- 保留最近30天的日志，自动删除早于30天的文件
- 日志格式：`[级别] [时间戳] [模块] 消息`

#### 6.4.3 前端错误捕获

| 捕获方式 | 处理 | 上报 |
|---------|------|------|
| `window.onerror` | 捕获未处理的运行时异常 | POST `/api/log` 上报后端 |
| `audio.onerror` | 捕获播放失败，关联歌曲ID | 本地记录+Toast提示 |
| `fetch` 异常 | 重试1次，仍失败则降级 | 下游触发WARN/ERROR日志 |
| 播放URL获取404 | 自动跳转下一首 | 关联歌曲ID写入后端日志 |

#### 6.4.4 浏览器兼容性

**Safari自动播放限制**（已知问题）：
- Safari 15+ 要求 `audio.play()` 必须在用户手势事件（click/touch）中调用
- 解决办法：所有播放操作均绑定在用户点击事件上（点击歌曲行→播放），不在JavaScript中自动触发
- 无需额外适配，因为本项目设计上就是"点击即播"，天然满足手势触发要求
- 追加后备：`document.addEventListener('click', () => audio.play(), { once: true })`

**其他兼容事项**：
- Firefox 90+：Web Audio API限制少于Safari，无特殊处理
- Chrome 90+：最兼容，无限制
- 所有浏览器：`<audio>` 标签使用 `preload="none"`，由用户点击触发加载

---

## 7. 安全与风控

### 7.1 网易云风控风险评估

> **核心结论：风控风险存在但可控。正常听歌（每天登录2-3次，手动操作，非自动化）属于中低风险，不是"用了就封"。真正被严打的是刷量/自动化脚本，不是普通播放行为。**

#### 7.1.1 风控检测机制

网易采用**多层检测系统**，不是单一因素触发：

| 检测层 | 检测手段 | 技术细节 |
|--------|---------|----------|
| **第1层：登录身份验证** | 客户端指纹检测 | 识别请求是否来自官方客户端，拦截非官方请求 |
| | 易盾(YiDun)验证 | 网易反欺诈SDK集成在登录流程，第三方客户端不支持此验证步骤 |
| | EApi加密头检测 | 网易加密API(eapi)有专用Header集，第三方客户端通常缺失 |
| | 错误代码8821 | 返回"需要行为验证码验证"，检测到非官方登录时触发 |
| **第2层：请求模式分析** | 请求频率分析 | 检测非人类播放速度（刷歌检测） |
| | IP信誉评分 | 云服务器IP被特别标记；未知/不常见IP触发风控 |
| | Cookie使用模式 | 检测来自第三方工具的Cookie认证模式 |
| **第3层：行为分析** | 播放模式检测 | "非法挂机行为"——检测无人值守的自动播放 |
| | 签到异常检测 | 多IP签到（签到脚本使用不同服务器）触发检测 |
| | 设备变化检测 | 频繁更换登录设备/IP触发风控 |

#### 7.1.2 不同行为的风险等级

| 行为场景 | 风险等级 | 典型后果 | 证据来源 |
|---------|---------|---------|----------|
| 批量刷歌（300首/天自动播放） | 🔴 极高 | "非法挂机行为"警告→封号 | Go SDK明确警告 "风控极为严格" |
| 第三方客户端+用户名密码登录 | 🔴 高 | 强制下线→改密码→3天冻结 | SPlayer、Melody项目确认 |
| 第三方客户端+二维码扫码登录 | 🟡 中 | 部分检测，比密码登录风险低 | YesPlayMusic用户报告 |
| 第三方客户端+Cookie/CookieCloud登录 | 🟢 中低 | 目前最安全的登录方式 | Go SDK推荐方案 |
| 正常听歌（登录后低频手动播放） | 🟢 低 | 登录后播放行为检测难度大 | 多源交叉验证 |
| 下载歌曲 | 🟢 低 | 不被视为风险行为 | Go SDK未标记任何风险 |
| 搜索歌曲 | 🟢 低 | 不需登录即可使用 | - |
| 获取榜单 | 🟢 低 | 不需登录即可使用 | - |

#### 7.1.3 封号是递进式的

网易不会一步到位永久封号，而是递进式惩罚：

```
首次触发 → 强制下线 + 要求改密码
     ↓ 再次触发（继续使用第三方客户端）
3天冻结
     ↓ 继续触发
7天冻结
     ↓ 屡次不改
更长时间冻结
     ↓ 极端情况（极罕见）
永久封号
```

> **永久封号极罕见**，多数用户在3-7天冻结后可恢复。

#### 7.1.4 真实封号案例

| 时间 | 平台 | 情况 | 后果 |
|------|------|------|------|
| 2024年10月 | YesPlayMusic用户 | 扫码登录后正常听歌 | 账号3天冻结 |
| 2024年12月 | YesPlayMusic用户 | 10月被封后继续用 | 再次冻结，用户表示"搞怕了" |
| 2026年4月 | 大规模事件 | 大量用户（含官方客户端用户）被冻 | 网易称"系统异常"，后解封 |
| 酷安用户 | 第三方客户端登录 | 收到"涉嫌违规"冻结通知 | - |

> **注意**：2026年4月大规模封号事件中，连官方客户端用户都中招——说明风控系统有时会误伤，不只有第三方用户会被冻。

#### 7.1.5 我们项目的风险定位

本项目典型用户场景：**主号登录，每天2-3次，正常听歌**。

| 环节 | 风险 | 说明 |
|------|------|------|
| **登录环节** | 🟡 中 | 二维码扫码比密码登录安全，但仍是主要检测点 |
| **搜索环节** | 🟢 低 | 无需登录，不触发风控 |
| **播放环节** | 🟢 低 | 正常频率播放，检测概率极低 |
| **数据验证** | 🟢 极低 | 仅启动时批量验证歌曲ID，请求量极小 |

**总结**：登录是唯一的中风险环节。一旦登录成功且未被即时拦截，后续正常播放几乎不会被检测。

### 7.2 三层防护体系

#### 第1层：降低登录检测风险（最关键）

| 措施 | 实现方式 | 优先级 |
|------|---------|--------|
| 二维码扫码登录 | 已确认方案，比密码登录安全 | P0 |
| 登录后保持会话 | Cookie存内存，不频繁重新登录，会话过期前持续使用 | P0 |
| 模拟官方客户端Header | 参考HyPlayer做法，在关键请求中伪造User-Agent等Header | P0 |
| 登录频率限制 | 同一账号5分钟内最多登录1次 | P1 |

#### 第2层：降低播放行为检测风险

| 措施 | 实现方式 | 优先级 |
|------|---------|--------|
| 请求限速 | 控制API调用频率，不超过正常人类节奏（60请求/分钟） | P0 |
| 数据缓存 | 搜索结果缓存24小时，播放URL缓存2小时，减少重复请求 | P0 |
| 不自动播放 | 进入页面不自动播放，需用户手动点击 | P0 |
| 无刷歌功能 | 不做"自动播放全部"之类的自动化功能 | P0 |

#### 第3层：高级防护（后续迭代）

| 措施 | 实现方式 | 优先级 |
|------|---------|--------|
| Header注入（HyPlayer方案） | 用户通过mitmproxy从官方客户端捕获真实Header/EApiHeaders，注入到后端 | P2 |
| CookieCloud同步 | 支持从CookieCloud导入官方客户端Cookie，避免直接登录 | P2 |
| 只读降级模式 | 如果登录风控过严，可降级为无登录模式（仅搜索+榜单，不可播VIP歌曲） | P2 |

### 7.3 登录安全设计

| 设计项 | 方案 | 理由 |
|--------|------|------|
| 登录方式 | 仅二维码扫码 | 比密码登录更安全，风控检测率更低 |
| Cookie存储 | 仅内存，不落盘 | 关闭浏览器即失效，减少泄露风险 |
| Cookie有效期 | 会话持续期间保持有效 | 减少重复登录次数 |
| 无自动登录 | 每次启动需重新扫码 | 避免长期存留Cookie |
| 风险提示 | 登录页温和提示 | "本项目为第三方客户端，极小概率触发平台风控" |

### 7.4 前端安全

| 设计项 | 方案 |
|--------|------|
| XSS防护 | 所有动态内容转义 |
| CSRF | SameSite Cookie |
| API密钥 | 无需API Key，所有请求走后端代理 |

### 7.5 数据冷启动设计

| 场景 | 前端行为 | 后端行为 |
|------|---------|---------|
| 首次启动（验证中） | 显示"时光隧道加载中..."动画 | 读取JSON + 批量验证 |
| 验证超时（>10秒） | Toast提示"部分歌曲状态待确认"，显示已加载数据 | 停止验证，返回当前缓存 |
| 验证失败 | Toast提示"数据加载异常"，提供重试按钮 | 返回原始JSON数据（未验证） |
| 数据为空（某格子无歌曲） | 显示"该年代暂无歌曲"引导用户切换 | 返回空数组 |
| 验证全部完成 | 无额外提示，正常使用 | 返回已验证数据 |

---

## 8. GitHub参考项目研究

### 8.1 核心参考项目

#### YesPlayMusic (32.8k⭐)
- **仓库**: https://github.com/qier222/YesPlayMusic
- **参考价值**: ★★★★★ 网易云API集成的最佳实践
- **关键技术**:
  - 二维码登录完整实现（`src/views/loginAccount.vue`）
  - 三层音源解析：缓存→网易云→UnblockNeteaseMusic
  - VIP检测：`freeTrialInfo !== null` 判断
  - 音频引擎：Howler.js + Blob URL内存管理
  - Cookie管理：`MUSIC_U` + `__csrf`，localStorage备用
- **我们借鉴**:
  - 二维码登录流程
  - VIP检测逻辑
  - Cookie管理方案
  - 音频引擎架构（简化版）

#### NeteaseCloudMusicApiEnhanced (880⭐)
- **仓库**: https://github.com/NeteaseCloudMusicApiEnhanced/api-enhanced
- **参考价值**: ★★★★★ 网易云API的核心依赖
- **关键信息**:
  - 原版已归档，此为社区活跃fork
  - npm包：`@neteasecloudmusicapienhanced/api@4.32.0`
  - YesPlayMusic已切换至`@neteaseapireborn/api`
  - 391个API模块，我们需要的全部存在
  - 内置UnblockNeteaseMusic支持

#### MusicFreeDesktop (8.3k⭐)
- **仓库**: https://github.com/maotoumao/MusicFreeDesktop
- **参考价值**: ★★★★ 多音源插件化架构
- **关键技术**:
  - 14方法统一插件接口
  - 质量降级重试循环（super→high→standard→low）
  - 插件懒加载机制
  - 用户自定义变量（Cookie等）
- **我们借鉴**:
  - 音源适配器接口设计（简化版）
  - 播放URL质量降级逻辑
  - 后续QQ音乐集成时的插件化思路

#### Music Time Machine (1⭐)
- **仓库**: https://github.com/suvarchaw/Music-Time-Machine
- **参考价值**: ★★★★ 几乎同概念产品
- **关键差异**: 仅Billboard数据，无中国平台，无播放功能
- **我们借鉴**: 年代主题视觉切换的设计思路

#### Interactive Music Timeline Explorer
- **地址**: https://gmanthenoxus.github.io/builds/music-timeline-explorer/
- **参考价值**: ★★★ 交互式时间线可视化
- **我们借鉴**: D3.js时间线交互模式（但我们用原生实现，不用D3）

### 8.2 不参考的项目

| 项目 | 原因 |
|------|------|
| Spotify Timeline | 国内不可用Spotify |
| MusicLine | 协作功能不需要 |
| Evolution of Mainstream Music | R/Python分析项目，非Web应用 |

---

## 9. 项目结构

```
music-time-machine/
│
├── README.md                          # 项目说明
├── LICENSE                            # MIT协议
├── package.json                       # Node.js依赖
│
├── start.sh                           # Mac/Linux启动脚本
├── start.bat                          # Windows启动脚本
│
├── server/                            # 后端服务
│   ├── index.js                       # 主入口，启动Express服务
│   │
│   ├── routes/                        # API路由
│   │   ├── auth.js                    # 登录/二维码/状态/退出
│   │   ├── songs.js                   # 歌曲列表查询
│   │   ├── play.js                    # 播放URL获取
│   │   └── meta.js                    # 年代/地区/风格元数据
│   │
│   ├── services/                      # 业务服务
│   │   ├── curator.js                 # 策展数据加载+启动验证+Mix组合
│   │   └── cache.js                   # 内存缓存(验证结果+播放URL)
│   │
│   ├── sources/                       # 音源适配器
│   │   ├── base.js                    # 适配器基类(接口定义)
│   │   ├── netease.js                 # 网易云适配器（含Cookie管理）
│   │   └── qqmusic.js                 # QQ音乐适配器（vkey→CDN URL完整链路）
│   │
│   └── utils/                         # 工具函数
│       ├── logger.js                  # 日志
│       └── rateLimiter.js             # API请求限速
│
├── client/                            # 前端
│   ├── index.html                     # 主页面
│   │
│   ├── css/
│   │   └── style.css                  # 全部样式（CSS变量+登录+滑块+播放器+列表+动画）
│   │
│   └── js/
│       ├── app.js                     # 应用主逻辑+认证+UI控制+列表渲染
│       ├── player.js                  # 播放引擎（独立状态机：播放/暂停/进度）
│       └── tunnel.js                  # 星际隧道Canvas动画（纯视觉层）
│
├── data/                              # 策展数据目录
│   ├── songs.json                     # 策展歌曲数据集(主文件，336个slot)
│   └── backups/                       # 自动备份（最多保留10份）
│
├── test/                              # 测试
│   ├── unit/                          # 单元测试（schema, cache, rateLimiter, curator, store）
│   └── integration/                   # 集成测试（routes: meta, songs, auth, play）
│
├── scripts/                           # 数据工具脚本
│   ├── core/                          # 核心共享逻辑
│   │   ├── schema.js                  # 共享常量+slot key解析（唯一来源）
│   │   ├── store.js                   # songs.json读写+备份+mergeSlots
│   │   └── api.js                     # 网易云API封装（搜索+验证+重试+限速）
│   ├── bulk-fill.js                   # 批量填充脚本
│   ├── search-helper.js               # 歌曲ID搜索辅助(调用core/api.js)
│   ├── validate.js                    # 数据验证(使用core/模块)
│   └── fill-template.js               # 空格子模板生成器
│
├── ecosystem.config.js                # PM2生产部署配置
```

---

## 10. 开发阶段与里程碑

### Phase 1-4: 核心功能 ✅ (全部完成)

| 任务 | 产出 | 验收标准 |
|------|------|---------|
| 初始化项目+依赖 | package.json | `npm install` 成功 |
| 集成NeteaseCloudMusicApiEnhanced | sources/netease.js | API服务启动成功 |
| 实现二维码登录 | routes/auth.js | 扫码可登录，获取Cookie |
| 实现登录状态检查 | routes/auth.js | 返回用户昵称头像 |
| 实现退出登录 | routes/auth.js | Cookie清除 |
| 实现Cookie过期处理 | routes/auth.js | Cookie过期时自动提示重新扫码，而非静默失败 |
| 实现搜索接口 | routes/songs.js | 关键词搜索返回结果 |
| 实现播放URL获取 | routes/play.js | 返回可播放的音频URL |
| 实现API限速 | utils/rateLimiter.js | 60请求/分钟限制 |
| 启动脚本 | start.sh / start.bat | 双击启动后端+开浏览器 |

**Phase 1 交付物**: 可登录+可搜索+可获取播放URL的后端服务

### Phase 2: 策展数据工具链 (预计2天框架 + 数据填充并行)

| 任务 | 产出 | 验收标准 |
|------|------|---------|
| 定义songs.json Schema | data/songs.json 结构定义 | JSON可被validate.js校验通过 |
| 实现数据加载器 | server/services/curator.js | 读取JSON→验证→内存缓存 |
| 实现启动时验证 | server/services/curator.js | 失效歌曲自动替换，空格标记 |
| 实现Mix组合逻辑 | server/services/curator.js | 跨风格随机组合10首 |
| 修改歌曲列表API | server/routes/songs.js | 从策展数据返回而非引擎 |
| 搜索辅助脚本 | scripts/search-helper.js | 输入歌名+歌手→输出网易云ID候选 |
| 验证脚本 | scripts/validate.js | 检查JSON完整性+歌曲有效性 |
| 空格子模板生成器 | scripts/fill-template.js | 生成待填格子的JSON骨架 |
| 填充首批13个高密度格子 | data/songs.json | 手填验证，确保ID准确可播 |

**Phase 2 交付物**: 策展数据框架 + 首批13个高密度格子的可用数据

> **数据填充节奏**：Phase 2框架2天完成，首批13个格子与Phase 3前端开发并行填充。剩余格子等项目跑通后逐步补全。

### Phase 3: 前端UI (预计5天)

| 任务 | 产出 | 验收标准 |
|------|------|---------|
| 星际隧道Canvas动画 | js/tunnel.js | 星空粒子+光带流动 |
| 光带颜色随年代变化 | js/tunnel.js | 7种年代配色 |
| 登录页UI | style.css + app.js | 二维码显示+扫码跳转 |
| 时间滑块 | style.css + app.js | 5年一格，拖拽/点击 |
| 地区Tab | app.js | 欧美/香港/大陆切换 |
| 风格Chip(含Mix) | app.js | 6个Chip切换 |
| 歌曲列表 | style.css + app.js | 10首歌+风格标签 |
| 底部播放器 | style.css + player.js | 进度条+音量+播放暂停 |
| VIP标记与提示 | player.js | 👑标记+Toast提示 |
| VIP切源Mock | player.js + play.js | VIP歌曲提示"切换音源"后播放备份歌曲 |
| 自动播放下一首 | player.js | 播完自动切歌 |
| 交互动画 | style.css + app.js | 列表刷新/Chip切换/过渡 |

**Phase 3 交付物**: 完整交互式Web界面

### Phase 4: 整合测试与发布 (预计3天)

| 任务 | 产出 | 验收标准 |
|------|------|---------|
| 前后端联调 | - | 所有功能端到端可用 |
| 边界情况处理 | - | VIP/无音源/空列表/网络断开 |
| Safari自动播放兼容 | - | 用户手势触发播放正常 |
| 冷启动状态测试 | - | 启动时加载动画+验证超时降级正常 |
| 错误日志验证 | - | 日志分级写入+轮转正常 |
| 启动脚本完善 | start.sh / start.bat | Mac/Windows双平台 |
| README编写 | README.md | 安装/使用/架构说明 |
| GitHub仓库初始化 | - | 推送代码，MIT协议 |

**Phase 4 交付物**: 可交付的开源项目

### Phase 5: 扩展增强 + 测试 + 部署 ✅

| 任务 | 状态 | 说明 |
|------|------|------|
| QQ音乐音源真实接入 | ✅ | 搜索→songmid→vkey→CDN URL完整链路，含地域检测自动降级 |
| 年代二级选择器 | ✅ | 5年格下方精确年份按钮（如1980→1980-1984子选择） |
| 地区扩展 | ✅ | 全部6个地区：欧美/香港/大陆/台湾/日本/韩国 |
| 风格扩展 | ✅ | 全部9个风格：Mix/摇滚/民谣/流行/R&B/电子/古典/嘻哈/爵士 |
| 策展数据填充 | ✅ | 336个slot全部10首歌，含自动验证+替补池 |
| 代码审查修复 | ✅ | 2 P0 bugs + 3 P1 issues + 4 P2 improvements |
| 测试覆盖 | ✅ | 63个测试（43 unit + 20 integration），89%+行覆盖率 |
| macOS本地生产部署 | ✅ | PM2进程管理 + launchd开机自启，端口3000 |
| Cookie管理修复 | ✅ | checkQrStatus保存cookie→song_url_v1携带cookie，VIP完整播放 |
| scripts/core/抽取 | ✅ | schema.js/store.js/api.js共享逻辑，消除脚本间重复 |

---

## 11. 风险与对策

### 11.1 技术风险

| 风险 | 严重程度 | 概率 | 对策 |
|------|---------|------|------|
| **网易云账号冻结** | 🟡 中 | 🟢 低 | 三层防护体系（见7.2）；正常听歌风险低；递进式封号不会一步到位；极端情况可只读降级 |
| NeteaseCloudMusicApiEnhanced停止维护 | 🟡 中 | 🟢 低 | 多个活跃fork备选；核心API稳定 |
| 网易云API接口变更 | 🟡 中 | 🟡 中 | 适配器模式隔离；快速切换fork |
| 中文歌曲元数据不完整 | 🟡 中 | 🟡 中 | 多源验证；人工补填关键词 |
| CORS问题 | 🟢 低 | 🟢 低 | 后端代理，前端不直接请求 |

### 11.2 数据风险

| 风险 | 严重程度 | 概率 | 对策 |
|------|---------|------|------|
| 某年代×地区×风格组合无数据 | 🟡 中 | 🟢 高 | 前端自动隐藏空风格；Mix模式跳过 |
| 网易云曲库缺失某些经典歌曲 | 🟡 中 | 🟡 中 | QQ音乐源补充；人工补填 |
| 风格分类不准确 | 🟢 低 | 🟡 中 | 人工抽检+关键词模板调优 |

### 11.3 产品风险

| 风险 | 严重程度 | 概率 | 对策 |
|------|---------|------|------|
| 版权争议(开源项目) | 🟡 中 | 🟡 中 | MIT协议+免责声明；不存储音频文件 |
| 用户因风控封号投诉 | 🟡 中 | 🟢 低 | 温和风险提示；递进式封号有时间反应；三层防护降低概率 |

---

## 12. 附录

### 12.1 确认决策记录

| 序号 | 议题 | 决策 | 确认时间 |
|------|------|------|---------|
| 1 | 地区范围 | 初期欧美/香港/大陆；预留台湾/日本/韩国 | 2025-07 |
| 2 | 风格范围 | 初期摇滚/民谣/流行/R&B/电子+Mix；预留古典/嘻哈/爵士 | 2025-07 |
| 3 | 视觉风格 | 星际隧道风，光带颜色随年代变化 | 2025-07 |
| 4 | 播放方式 | 必须嵌入播放，点击即播 | 2025-07 |
| 5 | 音频方案 | 网易云账号登录+完整播放，VIP自动切换QQ源 | 2025-07 |
| 6 | 登录方式 | 仅二维码扫码，不存密码 | 2025-07 |
| 7 | 数据策略 | 平台榜单/搜索为主，非用户歌单 | 2025-07 |
| 8 | 年代范围 | 1970-2000先行 | 2025-07 |
| 9 | 项目名 | music-time-machine | 2025-07 |
| 10 | 仓库/协议 | GitHub + MIT | 2025-07 |
| 11 | 启动方式 | 双击脚本→后端→自动开浏览器 | 2025-07 |
| 12 | 离线模式 | 不支持 | 2025-07 |
| 13 | 收藏/历史 | 不做 | 2025-07 |
| 14 | Mix模式 | 默认选中，跨风格随机组合10首，显示风格标签 | 2025-07 |
| 15 | 网易云API集成 | 子模块引入（后改用npm包） | 2025-07 |
| 16 | 风控评估结论 | 正常听歌(每天2-3次登录)属中低风险；不做"建议小号"；三层防护体系；递进式封号非一步到位 | 2025-07 |
| 17 | 数据方案 | 放弃AI归类引擎，改用策展数据（JSON预填+启动验证+替补池） | 2025-07 |
| 18 | 歌曲ID获取 | 混合方案：高密度格子手动确认，中等格子半自动+抽检，低频格子脚本批量+置信度标注 | 2025-07 |
| 19 | 歌曲失效处理 | 启动时验证+替补池自动顶入+数据不足坦诚提示，不回退到实时搜索 | 2025-07 |
| 20 | 前端文件组织 | 前期合并文件（CSS 7→1, JS 6→3），Phase 4稳定后视需拆分 | 2025-07 |
| 21 | 开发周期 | 每个Phase增加1-2天缓冲，总周期10-13天→15天（约3周） | 2025-07 |
| 22 | QQ音乐源 | Phase 3先做Mock降级验证体验，Phase 5做真实接入 | 2025-07 |
| 23 | 浏览器兼容 | 播放全量绑定用户手势，Safari自动播放限制不做额外适配 | 2025-07 |
| 24 | Cookie管理 | checkQrStatus(code=803)→保存res.body.cookie→song_url_v1携带cookie，VIP用户可完整播VIP歌曲 | 2026-05 |
| 25 | 测试框架 | Node.js内置node:test（零依赖），supertest做HTTP集成测试，依赖注入+工厂模式支持测试隔离 | 2026-05 |
| 26 | 生产部署 | macOS用PM2进程管理+launchd开机自启，ecosystem.config.js控制内存上限200M | 2026-05 |
| 27 | scripts/core抽取 | schema/store/api三个共享模块消除bulk-fill/validate/search-helper间重复，SLOT_KEY_RE动态生成 | 2026-05 |
| 28 | QQ音乐接入策略 | 直接HTTPS调用QQ音乐公开API(search→songmid→vkey→CDN URL)，含地域检测(中国IP白名单)自动降级 | 2026-05 |
| 29 | 搜索结果统一 | search-helper.js改用core/api.js的searchCandidates()，消除两份搜索逻辑 | 2026-05 |

### 12.2 NeteaseCloudMusicApi关键端点

| 端点 | 路由 | 用途 |
|------|------|------|
| QR Key | `/login/qr/key` | 获取二维码Key |
| QR Create | `/login/qr/create` | 生成二维码图片 |
| QR Check | `/login/qr/check` | 检查扫码状态 |
| Search | `/cloudsearch` | 搜索歌曲/专辑/歌手 |
| Song Detail | `/song/detail` | 歌曲详情 |
| Song URL | `/song/url/v1` | 获取播放链接 |
| Artist Detail | `/artist/detail` | 歌手详情 |
| Playlist Detail | `/playlist/detail` | 歌单详情 |
| Toplist | `/toplist` | 所有榜单 |
| Login Status | `/login/status` | 登录状态 |
| Logout | `/logout` | 退出登录 |

### 12.3 年代配色方案

| 年代 | 主色 | 辅色 | CSS变量 |
|------|------|------|---------|
| 1970 | #E8833A | #8B5E3C | `--era-primary`, `--era-secondary` |
| 1975 | #C62828 | #FFB300 | |
| 1980 | #FF1493 | #7B68EE | |
| 1985 | #00BFFF | #9400D3 | |
| 1990 | #1A237E | #00C853 | |
| 1995 | #90CAF9 | #E0E0E0 | |
| 2000 | #F5F5F5 | #B3E5FC | |

### 12.4 测试策略

| 项目 | 选择 |
|------|------|
| 框架 | Node.js 内置 `node:test`（Node 18+ 原生，零依赖） |
| HTTP测试 | `supertest`（仅1个dev依赖） |
| 覆盖率工具 | Node 22 内置 `--experimental-test-coverage` |
| Mock策略 | 依赖注入（路由工厂模式天然支持）+ flag变量控制mock行为 |

**测试分层**:

```
test/
├── unit/                          # 43 tests — 纯函数/算法
│   ├── schema.test.js             # 10 tests: parseSlotKey, formatSlotKey, SLOT_KEY_RE, 常量
│   ├── cache.test.js              # 12 tests: set/get/ttl/has/delete/clear
│   ├── rateLimiter.test.js        # 6 tests: token bucket + middleware
│   ├── curator.test.js            # 8 tests: getSongs, _generateMix (genre capping, max 10)
│   └── store.test.js              # 7 tests: mergeSlots (add/new/overwrite/immutable)
├── integration/                   # 20 tests — Express路由层
│   ├── routes-meta.test.js        # 3 tests: eras/regions/genres
│   ├── routes-songs.test.js       # 5 tests: valid query, year filter, mix, 400, empty
│   ├── routes-auth.test.js        # 5 tests: QR flow (key/create/check/status)
│   └── routes-play.test.js        # 7 tests: netease/QQ fallback/cache/404/400/VIP
```

**覆盖率** (2026-05):

| 文件 | 行% | 分支% | 函数% |
|------|-----|-------|-------|
| scripts/core/schema.js | 100% | 100% | 100% |
| server/services/cache.js | 100% | 100% | 100% |
| server/utils/rateLimiter.js | 100% | 100% | 100% |
| server/routes/play.js | 95.9% | 73.5% | 100% |
| server/routes/meta.js | 100% | 100% | 100% |
| server/routes/songs.js | 93.6% | 91.7% | 100% |
| server/routes/auth.js | 79.0% | 36.8% | 83.3% |
| **全部** | **89.5%** | **90.9%** | **92.4%** |

运行命令: `node --test test/unit/*.test.js test/integration/*.test.js`
覆盖率: `node --experimental-test-coverage --test test/unit/*.test.js test/integration/*.test.js`

### 12.5 本地生产部署

**部署方案**: PM2 进程管理 + macOS launchd 开机自启

```bash
# 启动
pm2 start ecosystem.config.js

# 状态查看
pm2 status
pm2 logs musictm

# 重启/停止
pm2 restart musictm
pm2 stop musictm

# 开机自启配置（需sudo）
sudo env PATH=$PATH:/usr/local/bin $(which pm2) startup launchd -u xj --hp $HOME
pm2 save
```

**ecosystem.config.js**:
```js
module.exports = {
  apps: [{
    name: 'musictm',
    script: 'server/index.js',
    cwd: '/Users/xj/projectxjai/claude/code/musictm',
    env: { PORT: 3000, NODE_ENV: 'production' },
    max_memory_restart: '200M',
    autorestart: true,
    watch: false,
  }],
};
```

---

> **文档状态**：v2.0  
> **下一步**：GitHub仓库初始化 → 服务器部署
